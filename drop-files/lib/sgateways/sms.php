<?php

/**
 * Use these variables:
 * 
 * $dest_num - Destination phone number where SMS will be sent
 * $message - Translated message containing the OTP code
 * $otp_code - The OTP code string only. Use this in your own custom message
 * 
 */

 //$message = urlencode($message);
 //$dest_num = substr($dest_num,0,1) == "+" ? substr($dest_num,1) : $dest_num;